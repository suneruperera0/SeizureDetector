# Infinite Loop
import time 

data_list = []
# basal_rate_low = 90
# basal_rate_high =

def calc_avg(data_list):
    rolling_avg = sum(data_list) / len(data_list)
    return rolling_avg

while True: #runs infinitely
    for rolling_avg in calc_avg:
        if rolling_avg > basal_rate and rolling_avg < basal_rate:
            #append list
            #output1
            #output2
        elif button = ON

